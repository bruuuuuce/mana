# Junie Setup

Use Junie inside the IDE for local code changes, test generation, local fixes, green-border execution, and developer feedback loops.

Junie should consume the active `.mana` workspace created by the framework. It reads planning and green-border artifacts before editing code and writes local evidence back to the workspace.
