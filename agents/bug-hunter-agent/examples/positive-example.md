# Positive example

Target only `src/orders/idempotency-handler.java` and its unit test. The user
asks for latent defects in retry handling, independent of the current branch.
The agent records the two-file scope, traces duplicate-request handling, and
reports a finding only if the source path and a proposed duplicate-delivery test
show the failure preconditions and observable duplicate charge.
