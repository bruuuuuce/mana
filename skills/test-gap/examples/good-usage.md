# Good Usage

- Run `test-gap` with complete inputs.
- Restrict scope with `test_levels` in fast local loops.
- Store the output as delivery evidence.
- Route blockers to `Developer / QA`.
- Re-run after material context changes.
