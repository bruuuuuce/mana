# Good Usage

- Run `story-quality` with complete inputs.
- Provide sibling stories when consistency findings are needed.
- Store the output as delivery evidence.
- Route blockers to `BA / Team Leader`.
- Re-run after material context changes.
