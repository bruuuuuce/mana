# Good Usage

- Run `acceptance-criteria-testability` with complete inputs.
- Store the output as delivery evidence.
- Route blockers to `BA / QA / Team Leader`.
- Re-run after material context changes.
