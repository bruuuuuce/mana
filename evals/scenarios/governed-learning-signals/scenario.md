# Governed Learning Signals

Provide two executions with `evidence.missing` for the same profile and one
execution that completes. Candidate collection must aggregate recurrence,
retain completion as counter-evidence, and produce only a candidate. Review
must create a learning-agent artifact; it must not modify a skill, profile,
guard, Service Context, or knowledge card.
