schema_version: 1
project_root: "__MANA_FIXTURE_PROJECT_ROOT__"
target:
  reference: "fixture-db"
  classification: "isolated_test"
verifications:
  - id: "unsafe-db-query"
    kind: "database_read"
    adapter: "postgresql"
    connection_env: "MANA_FIXTURE_DATABASE_URL"
    query_file: "unsafe-query.sql"
    environment: "dedicated_test"
    execution_status: "runnable"
    safety: "requires_explicit_database_read_approval"
    timeout_seconds: 30
    approved: true
