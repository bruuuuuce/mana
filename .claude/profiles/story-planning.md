# Story Planning

Use Claude Code to load the relevant profile (`story-start`, `story-ready-for-dev`), run the matching agent, produce Markdown artifacts in the active `.mana` workspace, and request human approval for blockers.

When Jira MCP is unavailable, populate `templates/epic-story-pack.template.md` manually and pass it as the requirement source.
