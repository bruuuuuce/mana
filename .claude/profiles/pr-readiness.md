# PR Readiness

Use Claude Code to load the `pr-ready` profile, run the PR Readiness Agent, produce Markdown artifacts in the active `.mana` workspace, and request human approval for blockers. Do not commit the generated PR package automatically.
