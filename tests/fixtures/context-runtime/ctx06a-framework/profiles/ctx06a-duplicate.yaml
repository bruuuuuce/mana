name: ctx06a-duplicate
trigger: test_fixture
runner: codex
agents:
  - ctx06a-fixture-agent
skills:
  - ctx06a-fixture-skill
skill_activation:
  baseline:
    - ctx06a-fixture-skill
  conditional:
context_runtime:
  version: 2
  workspace_kind: session
  target_kind: pull_request
  human_gates: [GATE-owner-approval]
  pipeline:
    - id: classify
      ordinal: 1
      model_tier: economy
      output_schema: phase-checkpoint-v1
      subagents: forbidden
      evidence_kinds: [source]
      evidence_statuses: [complete]
      retry_limit: 1
    - id: classify
      ordinal: 2
      model_tier: economy
      output_schema: phase-checkpoint-v1
      subagents: forbidden
      evidence_kinds: [summary]
      evidence_statuses: [complete]
      retry_limit: 0
human_approval_requirement: true
