# Journey Model v0 fixture coverage

`tests/mana-journey.sh` creates a persisted fixture with host-generated IDs.
It covers linear execution, branch/join, runtime effects, concept occurrence,
rationale hypothesis, and a cycle/back-edge; it also checks anchor evolution,
deterministic materialization, and invalid-reference detection.
